import React from "react";
import Wrapper from "../../components/Wrapper/Wrapper";

// add title with react helmet later
const MyEvents = () => {
	return (
		<Wrapper>
			<h1>My Events</h1>
			<p>
				this page should just list all the events a user has had an basic information about
				them
			</p>
		</Wrapper>
	);
};

export default MyEvents;
